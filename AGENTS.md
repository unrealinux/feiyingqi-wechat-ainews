# AGENTS.md — 项目协作与安全发布规范

本文件是本项目（微信公众号 AI 资讯自动发布）的**强制作业规范**。任何 Agent 在本仓库执行发布类任务前，**必须先读完并遵守本文件**。本文件优先级高于默认行为。

---

## 0. 背景：为什么有这份规范（重要教训）

本号曾因**机器批量发布 + 模板化洗稿内容**触发微信平台限流/降权，表现为**推荐流量曝光暴跌**（已关注粉丝仍能收到，但算法推荐、"看一看"、搜索流量归零）。

复盘确认的踩坑行为（**严禁再犯**）：

- 生成 100+ 个 `publish_ang*.py` 脚本，每个一次发 3 篇，累计发布数百篇。
- 内容为"填词模板"：同一句式换名词反复复用（如"AI XX 不是'替代 XX'，而是'让你的 XX 更 XX'"）。
- 编造案例与数据：虚构人物（"小李""李奶奶"）+ 无来源数字（"60%""300万销售额"），批量复用。
- 大量发布后又批量删除（`delete_drafts*.py`），呈现内容农场/刷号特征。

**核心结论：低质批量内容 = 平台降权 = 曝光暴跌。本规范的全部目的，就是杜绝这类行为。**

---

## 1. 绝对禁止（红线，任何情况下都不做）

1. **禁止批量发布**：一次会话/一天内，禁止创建超过 **1 篇** 正式内容草稿。
2. **禁止模板化洗稿**：禁止用同一套 HTML 结构 + 同一套句式，仅替换关键词批量生产文章。
3. **禁止编造事实**：禁止虚构人物故事、伪造数据、编造"某专家说"。所有数据/案例必须真实且可溯源。
4. **禁止发布后再批量删除**：不要把公众号草稿箱当测试场。测试用本地预览，不要反复建/删线上草稿。
5. **禁止直接调用 `publish_draft()` 群发**：群发是不可逆的强操作，**必须经用户明确逐次授权**。
6. **禁止在未读本文件、未获用户确认的情况下运行任何 `publish_*.py`**。

---

## 2. 发布前强制检查清单（Checklist）

执行任何发布前，Agent 必须逐项确认（建议用 todo 跟踪）：

- [ ] 用户已**明确要求发布这一篇**（不是"继续"这类模糊指令连发）。
- [ ] 内容为**原创**：标题、结构、排版、句式与既有文章**明显不同**。
- [ ] 所有数据**有真实来源**，并在文中标注（如"据 XX 报告"）。
- [ ] 无虚构人物/编造案例；如用案例，须为真实公开可查的。
- [ ] 本次会话/当天发布数 **≤ 1 篇**。
- [ ] 已在本地生成 HTML 预览供用户确认，而非直接建线上草稿。
- [ ] 只创建**草稿**（`create_draft`），**不群发**（`publish_draft` 需单独授权）。

**任一项不满足 → 停止，向用户澄清，不要发布。**

---

## 3. 内容质量标准（避免被判低质）

- **频率**：稳定低频，建议每周 1–2 篇，**严禁日更海发**。
- **原创性**：每篇独立构思。标题公式、开头方式、小标题结构、配色排版都要有差异。
- **信息价值**：提供独家观点 / 真实数据（带来源）/ 深度分析，而非通用情绪拼贴。
- **去 AI 味**：成稿须人工润色，去掉"不是替代……而是让……更……"这类机械句式和空话套话。
- **真实性**：数据、案例、引用一律真实可查。宁可不写，不可编造。

---

## 4. 技术操作规范

### 环境
- 平台：Windows / PowerShell 5.1；从仓库根目录 `E:\Project\feiyingqi-wechat-ainews` 运行。
- 脚本编码：脚本开头需 `sys.stdout.reconfigure(encoding='utf-8')`（日志含中文）。
- 依赖导入：`sys.path.insert(0, 'src')` 后 `from publisher import WeChatPublisher`。

### 核心 API（`src/publisher.py`）
- `get_access_token()` — token 获取/刷新（自带缓存，勿高频调用）。
- `_upload_media(file_path, "image")` — 上传封面，返回 media_id。
- `create_draft(title, content, author, cover_media_id)` — **创建草稿**（默认允许的发布方式）。
- `publish_draft(media_id)` — **群发（强操作，需用户逐次明确授权）**。

### 已知注意事项
- 运行时会出现 `健康检查失败: 1 个检查` 警告，非致命，可忽略。
- IP 白名单：调用 API 的公网 IP 必须在公众号后台白名单内，否则报 `invalid ip ... not in whitelist`。换网络需先加白名单。
- 封面统一 1440×810，作者统一 `AI前沿观察`，生成图先存 `output/` 再上传。
- 字体加载顺序：`msyhbd.ttc → msyh.ttc → simhei.ttf`。

### 脚本管理
- **不要**再新建 `publish_ang*.py` 这类批量编号脚本。
- 一篇文章一个有语义命名的脚本；发布后可归档，不要堆积。

---

## 5. 若怀疑已被限流

1. 先看后台真实指标区分病因：**曝光暴跌**=平台降权；**打开率低**=内容问题；**掉粉**=读者反感。
2. 若确为曝光暴跌：**立即停止一切发布**（继续发只会加深降权）。
3. 转为低频、高质、纯原创，观察 2–4 周曝光是否回升。
4. 长期无起色时，如实告知用户"抢救旧号可能不如新号重做"，由用户决策。

---

## 6. 与用户交互原则

- "继续 / 再来 / 多生成" 等模糊指令，**不等于授权批量发布**。遇到时应先确认发布意图与数量。
- 发布是有平台后果的操作。**宁可多问一句，不可擅自海发。**
- 涉及群发、批量删除、换号等高影响操作，必须获得用户明确逐项确认。
